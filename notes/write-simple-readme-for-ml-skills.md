write a simple readme, suggest a diffent file name other than readme.md if you think that file nameis better
1: for I can go through this document in max 5 mins explain my ml-design and ml-modeling setting.
Elements to include:
1: brief intro
2: explain the overall design: invoke to use, step-wise documentation,
for execute, check output, then run next steps
3: a structure that quick show how to fork to ml-design path and then
converge, and an option/fork for parallel build models, and an early option to run/fork at the beginning for monkey-mode.

asking 3 questions are good, but need to get ready go with assumptions if
  user don't provode. for example if no user's input/guidance
  1: self-define the task from the prompmpt,
  2: if no data provided, generate reasonalble small synthetic data
  3: assume metrics based on yoru knowledge
  4: define a reasonble baseline. runnable should be minimum. assume a
  baseline number based on your problem definition and data you have.

